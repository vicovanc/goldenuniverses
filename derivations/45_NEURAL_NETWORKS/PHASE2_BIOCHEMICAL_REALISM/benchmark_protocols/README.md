# Benchmark Protocols

Each benchmark protocol must define:

- target metric
- reference range
- uncertainty model
- pass/fail threshold
- rejection implication

All benchmark scripts emit explicit pass/fail booleans for gate consumption.
