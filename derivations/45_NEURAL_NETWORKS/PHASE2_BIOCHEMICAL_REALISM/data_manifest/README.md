# Data Manifest Contract

Each manifest entry declares:

- `dataset_id`
- `source`
- `license`
- `acquisition_date`
- `preprocessing_hash`
- `split_policy`
- `intended_module`

Phase 2 currently uses deterministic synthetic baselines; this manifest is the contract for replacing them with external biology benchmarks.
