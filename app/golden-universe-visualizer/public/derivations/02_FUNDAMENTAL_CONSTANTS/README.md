# 02_FUNDAMENTAL_CONSTANTS

Status: active + archived legacy split.

## Canonical script

- `02_derive_constants_v2.py` (active, honest labeling: derived vs calibrated vs open).

## Archived legacy script

- `01_derive_all_constants.py` moved to:
  `derivations/archive/deprecated_fundamental_constants/01_derive_all_constants.py`
- Reason: exploratory structure with mixed assumptions; superseded by v2.

## Current canonical conventions used here

- `c_R = 188/(48π)` from SU(5)+SUSY counting.
- `M_0 = M_P / sqrt(4π c_R)`.
- `N_today = 143` (from κ₀ and CMB closure), not 200.
- `α_GUT` in `gu_constants.py` is calibrated from `α_EM`; hypothesis `1/(8πφ)` is retained only as falsified comparison.
